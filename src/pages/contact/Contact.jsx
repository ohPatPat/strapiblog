import { useEffect, useState } from "react"
import axios from "axios"
import './Contact.scss'
export const Contact = (props) => {
    return (
        <section id="Contact">
            <h1>{props.title}</h1>
        </section>

    )


}